# Локальный профиль боевого EIS (только DaData). Файл в .gitignore — не коммитить ключи в репозиторий.
spring.config.activate.on-profile=live

app.eis.stub-mode=false
app.eis.dadata.token=${DADATA_TOKEN:}
